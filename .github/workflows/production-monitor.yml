name: Production Monitor

on:
  schedule:
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-monitor
  cancel-in-progress: true

jobs:
  verify-production:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Verify public origin and governed release
        env:
          ORIGIN: https://mibvendor.io
          EXPECTED_DATA_RELEASE: license-signaled-2026-07-20.2
        run: |
          EXPECTED_VERSION=$(tr -d '\r\n' < VERSION)
          EXPECTED_COMMIT=$(./scripts/resolve-production-commit.sh)
          export EXPECTED_VERSION
          export EXPECTED_COMMIT
          ./scripts/verify-production.sh
